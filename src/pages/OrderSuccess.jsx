import { Link, useParams } from 'react-router-dom';
import { useOrders } from '../context/OrderContext.jsx';
import Button from '../components/common/Button.jsx';
import { currency } from '../utils/currency.js';
export default function OrderSuccess(){ const {id}=useParams(); const {getOrder}=useOrders(); const order=getOrder(id); if(!order)return <div className="site-container py-32">Order not found.</div>; return <section className="site-container flex min-h-[70vh] items-center justify-center py-20"><div className="w-full max-w-3xl rounded-[32px] border border-line bg-graphite p-8 md:p-12"><p className="eyebrow text-crimson">ORDER_CONFIRMED</p><h1 className="mt-4 display-tight text-7xl font-semibold md:text-9xl">YOU'RE IN.</h1><p className="mt-6 max-w-xl text-sm leading-7 text-silver">Your order has been created locally for the assessment demo. No real charge or fulfillment will occur.</p><div className="mt-8 grid gap-4 border-y border-line py-6 sm:grid-cols-3"><div><p className="eyebrow">Order</p><p className="mt-2 text-sm">{order.id}</p></div><div><p className="eyebrow">Status</p><p className="mt-2 text-sm">{order.status}</p></div><div><p className="eyebrow">Total</p><p className="mt-2 text-sm">{currency(order.total)}</p></div></div><div className="mt-8 flex flex-wrap gap-3"><Link to={`/account/orders/${order.id}`}><Button>View order</Button></Link><Link to="/shop"><Button variant="ghost">Continue shopping</Button></Link></div></div></section>; }
